<% if connectors.any? %>
  <% connectors.each do |connector| %>
    <%= link_to connector, routes.connector_path(connector) %>
  <% end %>
<% else %>
No connectors.
<% end %>

<hr>

<%= link_to "New connector", routes.new_connector_path %>
